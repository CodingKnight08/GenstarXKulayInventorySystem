name: Deploy to SmarterASP.NET via FTP

on:
  push:
    branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x' # or 6.0 depending on your project

    - name: Publish Server (includes Client + Shared)
      run: dotnet publish GenstarXKulayInventorySystem.Server/GenstarXKulayInventorySystem.Server.csproj -c Release -o ./publish



    - name: FTP Deploy
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: win6030.site4now.net
        username: codeknight-001
        password: P@ssw0rd!@#$
        local-dir: ./publish/
        server-dir: /wwwroot/
        dangerous-clean-slate: true

    - name: Verify site is live
      run: |
        sleep 20
        curl -I http://codeknight-001-site1.ktempurl.com/
        
    
